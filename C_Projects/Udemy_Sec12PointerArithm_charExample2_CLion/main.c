#include <stdio.h>
#include <string.h>

int main()
{
    char multiple[] = "a string";


    printf("some is %s\n", multiple);
    printf("size of some is %i\n", sizeof(multiple));
    printf("strlen of multiple is %i\n", strlen(multiple));

    return 0;
}
